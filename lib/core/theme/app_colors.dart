import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFFF0000);        // YouTube Red
  static const background = Color(0xFF0F0F0F);     // Near black
  static const surface = Color(0xFF181818);        // Dark grey
  static const surfaceVariant = Color(0xFF272727); // Lighter grey
  static const subtitleTop = Color(0xFFFFFFFF);    // White (English)
  static const subtitleBottom = Color(0xFFFFEB3B); // Yellow (Vietnamese)
  static const focusBorder = Color(0xFFFFFFFF);    // White focus ring
  static const seekBar = Color(0xFFFF0000);        // Red
  static const seekBuffer = Color(0x80FFFFFF);     // Semi-white
}
